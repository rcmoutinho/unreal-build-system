include:
  - ../docker-compose.network.yml

services:
  perforce:
    container_name: perforce
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    volumes:
      - perforce:/p4
    ports:
      - 1666:1666

volumes:
  perforce:
    name: perforce
    driver: local
